- name: Reload systemd
  systemd:
    daemon_reload: true

- name: Restart systemd service {{ systemd_service_name }}
  systemd:
    name: "{{ systemd_service_name }}"
    daemon_reload: true
    state: restarted